//Datatypes:=Data types are declarations for variables.
            // This determines the type and size of  data associated with variables which is
            // essential to know since different data  types occupy different sizes of memory.

//There are two types of datatypes
//1:-Premitive datatype(int(4),float(4),char(2),short(2),byte(1),double(8),long(8),boolean(1))
//2:-Non primitive datatype(array,string)
public class datatypes {
    public static void main(String[] args) {
        int a=10;
       char b='a';
       double c=23.43;
       boolean d=true;
       System.out.println(a);
        System.out.println(b);
         System.out.println(c);
          System.out.println(d);
        
    }
}
